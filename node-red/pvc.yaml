apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "node-red"
  namespace: "node-red"
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
